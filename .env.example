# 本地开发用的构建期兜底配置。
#
# 复制成 `.env.local` 再填值。`.env.local` 已被 `.gitignore` 忽略，不会入库。
#
# ## 去哪拿 appId
#
# 登录声网控制台 → 项目管理 → 创建或选择一个项目 → 复制「App ID」。
#   国内站：https://console.shengwang.cn/
#   国际站：https://console.agora.io/
#
# 只需要 App ID。**不要**把 App Certificate、临时 Token 或任何密钥写进来 ——
# 本项目不含 token 生成器，也不读取这两类值（见 README「三条必读声明」第 3 条）。
#
# ## 优先级
#
# 这里填的值优先级**最低**。运行时 `window.__ENV__.appId` 会覆盖它（见 README
# 「注入契约」）。顺序不可颠倒：`import.meta.env` 是构建期烧进 bundle 的常量，
# 若让它优先，嵌入本实验室的上层页面就永远换不掉 appId。
#
# 三者都没有时，应用渲染引导页而不是报错 —— 未配置不是异常。

VITE_APP_ID=

# 更新日志

「未发布」记录当前开发变更，发布时再按实际版本和日期归档。设计交接包的 v1.1／v1.2 与 `package.json` 中的 `0.1.0` 均不作为本日志的正式发布版本。

历史部分根据 `claude/ink-ui-redesign` 分支截至 `b2d0284` 的完整可达 Git 历史整理，覆盖 2026-08-06 至 2026-09-07 的 11 条提交；日期按 UTC+8 归并，内容结合提交说明与实际差异提炼。

## 未发布

更新至 2026-09-08。

### 新增

- 创建房间、按名称加入房间时可选填昵称。支持中文与 Emoji，按 NFC 规范化、去除首尾空白后最多 20 个 Unicode 码点；空值使用原角色默认名，允许重名，身份、邀请、麦位及封禁仍使用 UID。
- 两个入口分别保留昵称草稿，失败、取消或暂离后可修改重试。昵称贯穿创建、加入、creating 恢复和 URL 刷新，保持 V1／V2 链接兼容；头像显示首个完整字素，避免截断 Emoji。

### 界面调整

- 右侧 RTM 数据流改为时间倒序，最新记录在顶部；新记录、筛选变化及重新展开后定位顶部，可向下滚动查看历史。采集快照和公屏消息顺序保持原行为，同步设计系统与参考原型。
- 主区增加 72px 方格背景，语聊房卡片增加浅深主题投影，左右栏标题增加 16px 路线／脉冲图标。
- 数据流使用 surface 行底、1px 外框及 3px 类型色左框，行内通过 API／EVENT 标签区分类型；保留分类筛选胶囊计数与折叠栏总数，移除展开标题旁的分类汇总。
- 数据流按 comfortable 密度固定为 70px 行高。长摘要单行省略并支持悬浮查看全文，失败摘要和错误详情共用一行，完整诊断仍可查看。
- 数据流呼吸仅改变边框和光晕，持续 1.8s × 2，结束后恢复静态；保留减少动态效果设置。
- 公屏保留全部消息，可视高度限制为五条普通单行消息；更多历史上下滚动查看，长文本保留换行，新消息沿用滚到底部行为。
- 房主麦位移除“房主”可见文字，实心皇冠由 11px 放大至 22px，置于卡片上缘以避开头像；保留可访问名称与悬浮说明。

### 修复

- 退出或被下麦后撤销迟到的麦克风发布结果；媒体状态同步串行执行并合并重复触发，按最新麦位与静音状态收敛，避免退出后继续写入 Presence。
- RTC 异步操作绑定所属实例，失败或取消的入房会清理原实例并保留最初错误；迟到的轨道、订阅结果和旧实例事件不再影响当前房间。
- 重复退出共用清理结果，清理完成前拒绝重新入房，避免旧退订误伤新订阅。
- 房主的麦位、强制静音及公告写入共用本地队列，执行时读取最新状态，写入失败不阻塞后续操作，避免并发覆盖。
- 聊天发送成功仅清空仍对应该次提交的草稿，保留等待期间的新输入、Emoji 编辑和失败后的草稿。
- 初始化 Presence 写入成功后更新本端昵称映射，无需等待自己的状态事件回推；写入失败或离房后的迟到结果不更新，远端昵称仍由 Presence 事件同步。
- 中文组合输入的 Enter 不误提交；非法换行粘贴不被单行输入框静默删改，错误提示与提交按钮状态一致。
- 新增昵称字段后，低高度桌面入口可在原卡片内滚动，标题与提交按钮完整可达；保留手机页面滚动。

### 文档与验证

- 数据流倒序调整通过全部单元测试 423 项、相关桌面／手机浏览器测试 14 项及生产构建；覆盖乱序时间、同毫秒记录、筛选、历史滚动、新增置顶与折叠恢复。设计原型在桌面／手机、浅深主题下独立验证倒序与公屏回归。
- 补齐当前设计系统、主参考原型和交接说明，对齐 v1.2 视觉、昵称、公屏五行滚动、标题计数与皇冠增量；对照画板继续引用主原型，早期概念与配色候选明确标记为历史参考。同步两份开发约定中的旧 wash／无投影规则。
- 新增本更新日志，整理已有提交历史；同步场景昵称说明、名称加入与刷新规则，以及保持逐字一致的 `AGENTS.md`／`CLAUDE.md`。
- 设计同步单独通过主原型 20 种视图／主题／屏宽组合、10 个浅深对照画板，以及昵称、公屏、数据流、手机抽屉和减少动态效果的浏览器检查；设计稿均使用本地模拟数据。双入口一致性测试与生产构建通过。
- 昵称与异步修复阶段完整单元测试 422 项通过；最终界面调整的相关回归 103 项通过。最终完整浏览器测试 58 项通过、2 项既有跳过，生产构建通过；覆盖桌面／手机、浅深主题、消息历史与长文本、皇冠无重叠及原有交互。
- 真实 RTM 双端已验证同名不同 UID、昵称互见、双向聊天及双方刷新恢复，测试房间已解散。自动浏览器测试使用隔离替身，与真实 RTM 验证分别记录；本轮未进行真实通话质量验收。

## 2026-09-07：墨 · Ink 界面重构

提交：[b2d0284](https://github.com/Walking-Yu/new-rtm-demo/commit/b2d02841ed8bab70e68c4e9369281f5eb1b074c1)

- **新增**：浅色／深色主题切换与本地偏好保存；顶栏展示页面级 RTM 连接状态。
- **调整**：使用「墨 · Ink」设计系统统一入口、未配置引导、场景占位页和语聊房；颜色与组件样式收敛到 `src/app/styles.css`。
- **调整**：外壳采用固定宽度三栏，左右栏可收为 48px 窄栏；移除拖拽分隔器，小屏展开侧栏时使用抽屉。
- **调整**：重做麦位、房主管理面板、听众邀请卡和我的状态；体验路径只展示一级任务，详细指引按交互展开。
- **调整**：RTM 数据流用 API／事件配色区分行，筛选项同时显示色点与计数。
- **文档与验证**：纳入 `design/rtm-experience-hall/` 设计交接包，并同步主题、样式、界面及浏览器测试。

## 2026-09-06

### 名称加入、体验路径与解散清理

提交：[bc0811a](https://github.com/Walking-Yu/new-rtm-demo/commit/bc0811a21a479435a17c6765f31104edf22171a0)

- **新增**：通过 RTM 共享名称目录跨设备创建／加入房间；名称规范化后判重，目录记录创建中、开放、关闭状态及封禁名单。
- **新增**：目录使用版本条件写入与本轮创建标识，处理创建竞争、中断恢复和未知写入结果；同名重建生成新的 roomId，旧链接不能误入新房间。
- **新增**：五步语聊房体验路径，进度依据成功 API 调用与已消费的业务状态；清空数据流或切换房间保留当前场景进度。
- **调整**：导航收敛为七个一级场景，仅语聊房已实现；移除页面上的邀请复制、粘贴邀请和最近房间入口，保留已有完整 URL 的兼容解析。
- **修复**：名称房间等待权威房间状态就绪且目录开放后再允许交互；已加入端持续观察目录，封禁、解散或 roomId 变化时退出。
- **修复**：房主解散先确认目录关闭，再广播、清理本轮房间 Metadata 并返回入口；清理失败可针对原 roomId 重试，共享写入未确认时保留房间并提示错误。
- **文档与验证**：补充共享名称目录说明与回归测试；提交说明记录单元测试 324 项通过、浏览器测试 38 项通过／2 项跳过、生产构建通过（为该提交的历史验证记录）。

### 构建与测试基础设施

提交：[9b1888d](https://github.com/Walking-Yu/new-rtm-demo/commit/9b1888d3e620dc665347a2e9081975ce25ff1715)、[aeed85e](https://github.com/Walking-Yu/new-rtm-demo/commit/aeed85e41c7402ae973b60e875e1a2e93ae14314)；合并记录：[2985592](https://github.com/Walking-Yu/new-rtm-demo/commit/2985592961deb7566085c03058a907c2926c60a4)（PR #1）。

- **新增**：GitHub Actions 在推送与 PR 时执行单元测试、构建和浏览器测试，CI 使用 Node.js 22。
- **修复**：补齐 lockfile 中各平台可选原生依赖，使 Linux 构建机能够安装所需产物。
- **修复**：房间目录测试夹具改用相对当前时间的日期，避免超过七天保留期后失效；E2E 断言对齐默认显示连接事件的行为。
- **已回滚**：本组提交曾加入 Cloudflare Tunnel 和 Pages 配置，随后移除 `--tunnel`、Vite 隧道设置、`public/_redirects`、`.node-version` 及相关说明。当前代码保留通用 CI 和测试修复，未保留这些平台专用部署功能。

## 2026-08-31：局域网启动默认值

提交：[ccd1803](https://github.com/Walking-Yu/new-rtm-demo/commit/ccd18037918666f4431d4149592342eaa1f5cfc1)

- **调整**：`start-demo.sh` 默认监听 `0.0.0.0`，启动 HTTPS 8080 并打印局域网地址，默认不再自动打开浏览器。
- **新增**：`--http` 用于显式启动普通 HTTP；继续支持 `--https`、`--both` 和兼容参数 `--no-open`。
- **文档与验证**：同步启动说明、环境检查输出和脚本测试。

## 2026-08-23：架构交接文档同步

提交：[274c53c](https://github.com/Walking-Yu/new-rtm-demo/commit/274c53cf7f3e5c0d3ecb30045230873acbdd0117)

- **文档**：更新需求、实施映射和交接材料，使其对应单 Tab 单角色、页面级 RTM 会话、事件驱动 Storage 与 npm SDK 的实现；移除文档中沿用旧 Lock／同页双端架构的说明。本提交未修改应用代码。

## 2026-08-21：单角色语聊房与事件驱动状态

提交：[39220c4](https://github.com/Walking-Yu/new-rtm-demo/commit/39220c4cf1b3438ba86a1e50c20bcc55b8f94639)

- **调整**：语聊房由同页双端改为每个 Tab 一个角色、一个页面级 RTM client；登录前注册事件，离房只退订，页面卸载才登出。
- **调整**：房主／听众分别提供业务命名的 RTM 操作与入站事件处理；移除旧 orchestrator 和 Lock 路径，房间状态由 Storage 全量事件驱动，重连不重订阅、不主动补读。
- **新增**：八个麦位、页面级连接与房间入口状态、基于唯一 `data` 参数的房间链接，以及当时用于邀请兼容的本地房间目录。
- **修复**：上麦申请在发送前锁定等待态，避免快速连点重复申请；排麦申请与等待态采用 30 秒超时，P2P 发送失败和麦位被占用时清理等待或邀请状态。
- **调整**：麦位只记录归属，RTC 发布失败保留麦位；通过 Presence 同步昵称、自主静音和麦克风采集异常。房主暂时离开只显示离开状态，其他成员继续互动。
- **调整**：数据流记录页面级 login／linkState 和退出退订，角色离房后保留已有记录；Storage 与麦位更新摘要只展示变化。
- **调整**：`agora-rtm` 从本地 vendor 依赖切换为 npm 的 `^2.3.0`；移除入口页的内联体验 App ID，使用本地或部署环境注入。
- **新增**：使用 mkcert 的局域网 HTTPS 与 HTTP／HTTPS 双服务启动模式；补充语聊房源码导航、功能映射、协议时序和相应测试。

## 2026-08-11：统一场景实验室

提交：[e5f4cf7](https://github.com/Walking-Yu/new-rtm-demo/commit/e5f4cf73ed4c3ba4af87fedf5fd46dbc86c0fea1)、[47a9b5b](https://github.com/Walking-Yu/new-rtm-demo/commit/47a9b5bbd67b5045c0d7d5810b0b278bba602952)

- **调整**：收敛为根目录 `src/` 和单个 `index.html` 的场景实验室，移除仓库内旧独立语聊房和 legacy 入口；启动脚本直接运行根应用。
- **新增**：在实验室中接入当时的房主／听众同页双客户端语聊房，提供麦位协同、房间交互和合并后的 RTM 调用／事件时间线；其他规划场景保留占位入口。
- **新增**：空的 `.env.example` 配置模板、MIT 许可证与嵌入注入说明，补充场景、角色 RTM、时间线及 E2E 测试。
- **修复**：Playwright 使用独立 `e2e` mode，隔离开发者本地 `.env` 配置，稳定覆盖未配置分支。
- **文档与验证**：建立逐字同步的 `AGENTS.md`／`CLAUDE.md` 约定，规范场景注册信息，并添加仓库结构回归测试。

## 2026-08-06：初始代码导入

提交：[0d1ba5a](https://github.com/Walking-Yu/new-rtm-demo/commit/0d1ba5a2e67c8d32f2c2dbed7cacbb7a9f69e539)

- **新增**：独立语聊房 SPA、连接设置页、房主／听众双端界面，以及基于 Agora RTM 与 RTC 的语音、上麦申请／邀请、聊天、礼物、公告和成员治理演示。
- **新增**：启动脚本、单元测试、浏览器测试及设计、调研和实施文档；同时导入早期场景实验室代码。该初始结构后续由单入口实验室和单角色架构替代。

> 踢出、封禁和强制麦控均是 Demo 的客户端协作机制，不代表已实现服务端权限控制。历史条目描述对应提交的行为，当前实现与使用方式以 [README.md](README.md) 为准。

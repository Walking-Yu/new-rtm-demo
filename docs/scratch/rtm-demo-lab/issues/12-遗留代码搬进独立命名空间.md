# 12 — 遗留代码搬进独立命名空间，为新骨架腾出位置

**Blocked by:** None — can start immediately

**Status:** resolved

**What to build:** 遗留的 24 场景实验室仍可通过独立入口正常运行，同时根源码目录的顶层命名空间被腾空，新骨架可以占用它而不撞名。遗留实验室的全部测试在搬动前后结果完全一致。

关键事实（已核验）：遗留源码内所有跨目录 import 都是相对路径且全部落在根源码目录内部，整体平移不破坏它们之间的相对关系，只有少数外部引用（构建配置、入口页、TS 配置、脚本）需要改。**这是移动不是删除。**

- [x] 搬动前先跑一遍遗留测试并记下通过数，这是本票唯一的验收标准
- [x] 整体平移到 `legacy` 子目录，**不要逐个文件改 import**
- [x] 新建遗留专用入口页，原入口页指向新骨架入口（新骨架入口尚不存在，中间态开发服务器会 404，属预期）
- [x] 构建配置的测试 setup 路径与 include 范围指向新位置
- [x] 脚本调整：主开发命令跑新骨架，遗留实验室与独立语聊房 SPA 各留一条独立命令，都不删
- [x] 构建配置的入口列表同时包含新旧两个入口页，否则遗留入口不进产物
- [x] 遗留测试通过数与搬动前完全一致
- [x] 遗留页面仍能正常渲染

## Comments

步骤 2 实际移动 40 个文件，计划原写 39，漏数了样式文件。

步骤 5 的结果：35 个 pre-existing 测试全绿；其中 1 个编码了旧的脚本委托契约的测试按新契约重写为 3 个，故总数从 36 变 38。

步骤 6 的实际验证方式：本环境 localhost 探测不可用（沙箱内开发服务器与后续探测无法共存，探测一律返回 000），改用单入口构建遗留入口页作为等价且更强的验证 —— 它真正解析整个 import 图，任何断裂的相对路径都会让构建失败，且不依赖网络。结果：1810 个模块全部解析，构建成功，产物含遗留入口页与对应 chunk。临时配置与产物已清理。

未覆盖的部分：「浏览器里肉眼看渲染是否正常、控制台有无报错」这一条静态构建无法替代，留给用户在本机确认。构建通过已排除路径断裂这一类最可能的平移故障。

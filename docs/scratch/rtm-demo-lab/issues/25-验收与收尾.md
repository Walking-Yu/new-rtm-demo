# 25 — 验收与收尾

**Blocked by:** 23, 24

**Status:** ready-for-agent

**What to build:** 对着 spec 的三条不可越界规则逐条核对，如实写下「第二个场景一天产出」这条验收锚点的实测结论，并把建造中浮现的新问题回写进 spec。

- [ ] 核对规则一（两份 RTM 文件零运行时依赖）：检查只有纯类型 import
- [ ] 核对规则二（RTM 文件里只有调用顺序、没有业务规则）：人工过一遍
- [ ] 核对规则三（RTC 模块顶部注释与 README 都写明了例外理由）：人工过一遍
- [ ] 用听众端那一票记录的真实耗时评估验收锚点。如果复制改造明显超过预期，**如实记录并向用户提出调整建议，不要粉饰**
- [ ] 向用户报告可删除清单并等确认，**不要自行删除**：布局原型目录、遗留语聊房 SPA、遗留 24 场景实验室
- [ ] 提醒用户建立 git 基线（仓库仍零 commit），**不要自行 commit**
- [ ] 把建造中浮现的新问题回写进 spec 的未决项，尤其是两站域名白名单（仍待核实）、时间线是否需要媒体类节点（等实测）、RTC 模块是否需要直播模式与主播观众角色

## 明确不在本 effort 范围（不要顺手做掉）

- 其余 22 个场景的实现
- 场景说明文字与能力标签的底部可折叠区（骨架留位置，不实现内容）
- 屏幕共享
- token server 与 token 生成
- 摄像头能力的实际使用场景（接口做出来，语聊房不用）
